{
  "name": "Explorer Query",
  "script_path": "sudo -u cdist -i eq-ss",
  "description": "Build a cdist explorer query in the browser, or run one.",
  "group": "Cdist",
  "requires_terminal": false,
  "output_format": "html_iframe",
  "parameters": [
    {
      "name": "Action",
      "required": true,
      "param": "--action",
      "type": "list",
      "default": "builder",
      "description": "Query builder builds the expression; Run query executes it.",
      "values": ["builder", "run"],
      "values_ui_mapping": {
        "builder": "Query builder",
        "run": "Run query"
      }
    }
  ],
  "include": "/home/cdist/files.external/cdist-utils.git/scriptserver/include/eq-${Action}.json"
}
