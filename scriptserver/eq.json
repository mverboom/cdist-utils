{
  "name": "Explorer Query",
  "script_path": "sudo -u cdist -i eq-ss",
  "description": "Query cdist explorer output. The **Query builder** action opens an interactive builder; **Run query** executes a query (usually prefilled by the builder).",
  "group": "Cdist",
  "requires_terminal": false,
  "output_format": "html_iframe",
  "parameters": [
    {
      "name": "Action",
      "required": true,
      "param": "--action",
      "type": "list",
      "default": "builder",
      "description": "Query builder builds the expression interactively; Run query executes it.",
      "values": ["builder", "run"],
      "values_ui_mapping": {
        "builder": "Query builder",
        "run": "Run query"
      }
    }
  ],
  "include": "/home/cdist/files.external/cdist-utils.git/scriptserver/include/eq-${Action}.json"
}
